package com.cicconesoftware.tripsentinel;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
/** Starts the TripSentinel Spring Boot application. */
public class TripsentinelApplication {

	public static void main(String[] args) {
		SpringApplication.run(TripsentinelApplication.class, args);
	}

}
